package com.waffletoy.team1server.post

// ENUM 타입 정의
enum class Category {
    PLANNER, // 적절한 값을 여기에 추가
    FRONT,
    APP,
    BACKEND,
    DESIGN,
    DATA,
    MARKETER,
}

enum class Category2 {
    A,
    B,
    C,
}
